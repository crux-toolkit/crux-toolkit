#!/bin/bash
#------------
. ../SOURCEME
#------------

#------------
cd forward/; 
ln -fs $MS2;
python ../../submit_sequest.py `basename $MS2`
cd ..

#------------
cd random/; 
ln -fs $MS2;
python ../../submit_sequest.py `basename $MS2`
cd ..


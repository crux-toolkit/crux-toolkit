#!/bin/bash
#------------
. ../SOURCEME
#------------


#------------
mkdir -p forward
cd forward/; 
ln -fs $MS2;
python ../../submit_sequest.py `basename $MS2`
cd ..
#
##------------
mkdir -p random
cd random/; 
ln -fs $MS2;
python ../../submit_sequest.py `basename $MS2`
cd ..
#
##------------
#cd forward-all/; 
#ln -fs $MS2;
#python ../../submit_sequest.py `basename $MS2`
#cd ..
#
##------------
#cd random-all/; 
#ln -fs $MS2;
#python ../../submit_sequest.py `basename $MS2`
#cd ..
#------------
# cd forward-strict/; 
# ln -fs $MS2;
# python ../../submit_sequest.py `basename $MS2`
# cd ..

#------------
# cd random-strict/; 
# ln -fs $MS2;
# python ../../submit_sequest.py `basename $MS2`
# cd ..



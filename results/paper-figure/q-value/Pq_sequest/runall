#!/bin/bash
#------------
. ../SOURCEME
#------------

#------------
cd forward/; 
ln -fs $FASTA; ln -fs $MS2;
python /nfs/gs/home/lukall/bin/submit_sequest.py $MS2

#------------
cd random/; 
ln -fs $FASTA; ln -fs $MS2;
python /nfs/gs/home/lukall/bin/submit_sequest.py $MS2

